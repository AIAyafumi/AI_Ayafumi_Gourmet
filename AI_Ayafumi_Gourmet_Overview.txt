# AI_Ayafumi_Gourmet_Overview

## 1. 概要

正式名称：`AIアヤフミ（グルメ）`

開発プロジェクト名：`AI_Ayafumi_Gourmet`

「AIアヤフミ（グルメ）」は、**居酒屋・飲み屋探しに特化した専門AI**として今後発展させる。

設計イメージは、

**「AIアヤフミ（ラーメン）の居酒屋版」**

とする。

単純にWeb検索で近くの居酒屋を探すだけではなく、

- 店舗DB
- 距離検索
- 条件フィルター
- Web新店舗発見
- 営業状況確認
- 店舗情報の蓄積
- 成長DB
- AI判定
- キャッシュ
- 将来的な利用履歴・好み

などを組み合わせ、**使うほど店舗情報が蓄積され、検索品質が上がる居酒屋専門AI**を目指す。

---

# 2. 開発優先順位

AIアヤフミ（グルメ）の本格開発は、

**AIアヤフミ（ラーメン）の主要機能・検索基盤が完成した後**

に開始する。

現在はGourmetを大きく開発せず、既存コード・既存検索基盤を保管する。

基本方針：

```text
AIアヤフミ（ラーメン）
↓
検索・DB・成長DB・キャッシュ等の設計を完成
↓
安定運用
↓
そこで得た設計を整理
↓
AIアヤフミ（グルメ）へ横展開
↓
居酒屋専門AIとして再構築
```

Gourmetを先行して独自設計し直すのではなく、**Ramenで実証できた仕組みを再利用する**。

---

# 3. AIアヤフミシリーズ内での立ち位置

```text
                       AIアヤフミ
                    本人専用・中央AI
                          │
             必要に応じて専門AIを利用
                          │
          ┌───────────────┼───────────────┐
          ▼               ▼               ▼
 AIアヤフミ（料理） AIアヤフミ（グルメ） AIアヤフミ（ラーメン）
    家庭料理           居酒屋・飲み屋          ラーメン
   一般利用可能         一般利用可能          一般利用可能
```

基本思想：

- `AIアヤフミ` = 本人専用の中央AI・司令塔
- `AIアヤフミ（料理）` = 家庭料理専門AI
- `AIアヤフミ（グルメ）` = 居酒屋・飲み屋専門AI
- `AIアヤフミ（ラーメン）` = ラーメン専門AI

各専門AIは独立して開発・運用する。

専門AI本体をCoreへ直接コピーして統合しない。

将来的にはCore側のClient / Toolから必要に応じて利用できる構造を目指す。

---

# 4. 名称方針

名称は今後も、

**AIアヤフミ（グルメ）**

を維持する。

中身を居酒屋専門へ変更しても、

`AIアヤフミ（居酒屋）`

へ改名しない。

`グルメ` という名称をブランド名として維持しつつ、実際の専門領域は、

**居酒屋・飲み屋検索**

とする。

---

# 5. これまでのGourmetについて

AIアヤフミ（グルメ）は、これまで外食検索AIとして試作してきた。

現在までに確認できている主な既存機能：

- LINE Webhook
- 駅入力
- ジャンル選択
- ラーメン系統選択
- 人数
- 利用時間
- Hot Pepper検索
- Yahoo!検索
- Tavily Web検索
- 店舗候補統合
- 重複除去
- 距離フィルター
- 営業状況確認
- ユーザーID別会話保持
- `/test`
- PowerShellテスト

これらは削除せず、**将来の居酒屋専門Gourmetへ再利用できる試作資産**として残す。

ただし、現行構造をそのまま完成形とはしない。

Ramen完成後に、Ramen側で確定した検索・DB設計を基準としてGourmetを再設計する。

---

# 6. プロジェクト基本情報

ローカル：

`C:\Users\ayafumi\Desktop\AI_Ayafumi\AI_Ayafumi_Gourmet`

メイン：

`gourmet_bot.py`

システムプロンプト：

`prompts/gourmet_system_prompt.txt`

主なテスト関連：

- `test_gourmet.ps1`
- `test_hotpepper.py`
- `test_tavily.py`
- `test_yahoo.ps1`

本番基盤：

Render

LINE：

AIアヤフミ（グルメ）のLINEインターフェースを利用する。

---

# 7. 将来の基本コンセプト

完成形は、Ramenと同様に、

**検索するAIではなく、店舗データを蓄積して育つ専門AI**

を目指す。

概念：

```text
ユーザー条件
↓
既存店舗DB検索
↓
十分な候補があるか
│
├─ YES
│   ↓
│  距離・条件・営業状況・キャッシュ等で絞る
│
└─ NO
    ↓
   Web新店舗発見
    ↓
   店舗情報確認
    ↓
   成長DBへ保存
    ↓
   次回以降再利用
↓
AI判定
↓
候補提示
```

同じ地域・同じ店舗について、毎回すべてをWeb検索し直す構造は避ける。

---

# 8. Ramenから横展開する予定の設計

Ramen完成後、以下の考え方をGourmetへ移植する予定。

## 8-1. 高速な基本店舗DB

既知の店舗を高速に検索できるDB。

Ramen側のFastDB相当の考え方を居酒屋へ適用する。

## 8-2. 成長DB

Web検索等で新しく発見・確認した店舗を蓄積する。

同じ店舗を何度もWeb検索しない。

## 8-3. Web新店舗発見

DBだけで候補が不足したときにWeb検索する。

常にWeb検索を実行しない。

## 8-4. TTL

新店舗発見や外部情報確認について、一定期間はキャッシュを利用する。

同じ地域へ短時間に何度も同じ検索を行わない。

## 8-5. 候補数制御

候補が大量にある場合、全店舗へ高コスト処理を実施しない。

安い処理で候補を絞ってから、

- Web確認
- AI判定
- 詳細取得

などを上位候補だけへ実施する。

## 8-6. AI判定キャッシュ

同じ店舗・同じ評価軸について、必要以上にAIへ再判定させない。

## 8-7. 明示的タイムアウト

AI/API処理は必ず明示的なtimeoutを持つ。

1つの外部サービスで全体が長時間停止しない設計にする。

---

# 9. 居酒屋の主な検索カテゴリ候補

将来的な主な分類候補：

- 大衆居酒屋
- 焼き鳥
- 海鮮
- 刺身
- 串焼き
- 焼肉系居酒屋
- 餃子
- 日本酒
- 焼酎
- クラフトビール
- ワイン
- 個室
- 飲み放題
- 食べ放題
- 立ち飲み
- 1人飲み
- 深夜営業
- 安め
- その他

分類は開発開始時に再整理する。

最初から細かすぎる分類を固定せず、実際の検索ニーズを見ながら増やす。

---

# 10. 将来の検索条件候補

ラーメンより居酒屋は条件が多いため、以下を候補とする。

- 場所 / 駅
- 距離
- 人数
- 利用時間
- 予算
- 居酒屋タイプ
- 料理ジャンル
- 飲み物
- 個室
- 飲み放題
- 喫煙可否
- 1人利用
- グループ利用
- 深夜営業
- 営業中
- 定休日
- 予約可否

すべてを最初から実装するのではなく、利用価値が高い条件から追加する。

---

# 11. 将来のLINE UIイメージ

例：

```text
居酒屋を探す
↓
駅 / エリア
↓
人数
↓
何を重視する？
↓
ジャンル
↓
予算
↓
利用時間
↓
この条件で探す
```

「何を重視する？」候補例：

- 安さ
- 料理
- お酒
- 個室
- 1人飲み
- 飲み放題
- おまかせ

UIはRamen完成後に、Ramenで得た知見も使って再設計する。

---

# 12. 店舗データ候補

将来的に店舗ごとに保持したい情報候補：

```text
店舗名
住所
緯度
経度
最寄駅
駅からの距離
ジャンル
特徴
価格帯
営業時間
定休日
営業状況
予約可否
個室
喫煙情報
飲み放題
食べ放題
主な料理
主なお酒
情報ソース
最終確認日時
AI分類結果
```

全項目を初期実装する必要はない。

必要性の高い項目から導入する。

---

# 13. 情報ソース

現在の既存Gourmetで利用・検証している主な情報源：

- Hot Pepper
- Yahoo!
- Tavily
- Web情報

将来、Ramenで採用したデータ取得・キャッシュ・DB方式を参考に再構成する。

特定サービス1つへ依存しない。

---

# 14. 距離検索

現在のGourmetでは駅を基準にした距離フィルターを実装済み。

確認済みの例：

`京王堀之内駅`

```text
lat=35.624393
lon=139.400319
```

現在の試作では1000mを基本に距離フィルターを行っている。

将来の居酒屋版では、

- 500m
- 1000m
- 1500m
- 徒歩時間相当

なども検討する。

Ramen側の距離設計を参考に最終決定する。

---

# 15. 営業状況

既存Gourmetでは、

```text
open
closed
unknown
```

の営業確認を実装している。

将来的にも営業状況確認は重要機能として維持する。

ただし、毎回すべての店舗へWeb検索するのではなく、

- キャッシュ
- 最終確認日時
- 情報ソース
- TTL

を利用してAPI/Web検索回数を抑える構造を目指す。

---

# 16. AIモデル

既存Gourmetでは、

- Groq
- Gemini
- OpenRouter

を利用する構成を持つ。

最終的なモデル順・モデル名・timeout・再試行設計は、Ramenで安定した構成を参考に再決定する。

基本思想：

```text
第一候補AI
↓
timeout / 429 / 503 / API障害
↓
別AIへフォールバック
```

1つのAI API障害でGourmet全体を停止させない。

---

# 17. Free First

AIアヤフミシリーズ共通方針。

Gourmetでも、

**Free First / 必要になったら有料化**

を基本とする。

- 無料枠を優先する
- API呼び出しを抑える
- DBを活用する
- キャッシュを活用する
- 同じ情報を何度も取得しない
- Open Source / ローカル処理も検討する
- 無料で代替可能なら安易に有料サービスを増やさない

ただし、無料を優先しすぎて安定性・安全性・機能性を大きく損なわない。

---

# 18. RamenとGourmetの関係

RamenはGourmetの下位機能ではない。

GourmetもRamenの上位機能ではない。

両方とも独立した専門AI。

```text
AIアヤフミ（ラーメン）
= ラーメンという1ジャンルを深く扱う専門AI

AIアヤフミ（グルメ）
= 居酒屋・飲み屋を深く扱う専門AI
```

技術設計はRamenからGourmetへ横展開するが、サービスとしては独立させる。

---

# 19. 既存Gourmetの扱い

現在の `gourmet_bot.py` は削除しない。

既存コードは、

**居酒屋版Gourmetを作るための試作・部品**

として保持する。

Ramen完成前に大規模な書き換えは行わない。

必要な場合でも、

- 致命的な不具合修正
- 秘密情報対策
- 最低限の保守

に留める。

本格的な再設計はRamen完成後に行う。

---

# 20. Overview / WorkLog運用

## `AI_Ayafumi_Gourmet_Overview.txt`

Gourmetの最新仕様・構成・設計思想・ロードマップ・現在地をまとめる正本。

Git管理対象。

## `AI_Ayafumi_Gourmet_WorkLog.txt`

Gourmetの開発作業履歴。

Git管理対象。

基本：

```text
Overview
= 現在の正しい仕様・構成・設計・ロードマップ

WorkLog
= そこへ至るまでに何を実施したか
```

WorkLogは、

**上 = 古い / 下 = 新しい**

の時系列で末尾へ追記する。

---

# 21. 開発時の重要ルール

`gourmet_bot.py` を変更する場合は、変更前に必ず構文状態を確認する。

```powershell
python -m py_compile .\gourmet_bot.py
```

基本：

```text
変更前py_compile
↓
現在の実コード確認
↓
小さく変更
↓
変更後py_compile
↓
単体テスト
↓
必要ならAPI実測
↓
必要ならLINE / Render確認
↓
Git差分確認
↓
変更ファイルだけstage
↓
commit
↓
push
```

重要：

- 既存SyntaxErrorがある状態で機能追加しない
- 関数名・変数名を推測しない
- 実コードを確認してから変更する
- 文字化けだけでファイル破損と判断しない
- 大規模一括書き換えを避ける
- 正常に動いている機能を最優先で守る

---

# 22. PowerShellテスト方針

日本語テスト時：

```powershell
$env:PYTHONIOENCODING = "utf-8"
$env:PYTHONUTF8 = "1"
```

結果共有時：

```powershell
$result = <command> 2>&1

$output = if ($result) {
    $result | Out-String
} else {
    "出力なし"
}

$output | Set-Clipboard
$output
```

結果はクリップボードへ入れて共有する。

---

# 23. Git運用

シリーズ共通ルール：

- `git add .` は原則使わない
- 変更したファイルだけstageする
- `git status --short` を確認する
- `git diff --cached --name-status` を確認する
- 秘密情報がstageされていないことを確認する
- commit messageは変更内容に合わせる

例：

```powershell
git add .\AI_Ayafumi_Gourmet_Overview.txt
git add .\AI_Ayafumi_Gourmet_WorkLog.txt

git status --short
git diff --cached --name-status

git commit -m "Add Gourmet project documentation"
git push
```

---

# 24. 現在の開発状態

現在：

**Gourmet本体の本格開発は一旦保留。**

既存の外食検索試作コード・LINE・API連携は保持する。

現在の最優先プロジェクト：

**AIアヤフミ（ラーメン）**

Gourmetは、

**Ramen完成後に「ラーメンAIの居酒屋版」として再開する。**

---

# 25. 最新ロードマップ

## Phase 0｜既存Gourmet試作 ✅

既に実装・検証した主なもの：

- LINE
- 駅検索
- ジャンル検索
- 人数
- 利用時間
- Hot Pepper
- Yahoo!
- Tavily
- Web店舗発掘
- 重複除去
- 距離フィルター
- 営業確認
- 会話保持
- `/test`
- PowerShellテスト

これらは将来の再構築時に利用可能な資産として保持する。

## Phase 1｜Ramen完成待ち ← 現在ここ

Gourmetの大規模開発は停止。

AIアヤフミ（ラーメン）の、

- 店舗DB
- 成長DB
- Web新店舗発見
- TTL
- 候補数制御
- AI判定
- AI判定キャッシュ
- APIタイムアウト
- 検索速度改善
- 本番安定化

等を完成させる。

## Phase 2｜Ramen設計の共通化

Ramen完成後、

- 何を共通化できるか
- 何をGourmet固有にするか
- DB構造
- キャッシュ構造
- 検索パイプライン
- API利用方針

を整理する。

## Phase 3｜Gourmet居酒屋版 基盤再構築

Ramenの仕組みを参考に、

**居酒屋版検索エンジン**

を構築する。

候補：

- 居酒屋FastDB相当
- 居酒屋成長DB
- Web新店舗発見
- TTL
- 距離検索
- 重複除去
- 営業確認
- AI分類
- AI判定キャッシュ

## Phase 4｜居酒屋カテゴリ・条件

追加候補：

- 焼き鳥
- 海鮮
- 大衆居酒屋
- 日本酒
- 焼酎
- 個室
- 飲み放題
- 1人飲み
- 安め
- 深夜営業

## Phase 5｜LINE UI再設計

Ramenで得たUI・検索体験の知見を反映する。

## Phase 6｜品質・速度・コスト最適化

- 候補数制御
- API呼び出し削減
- DB優先
- キャッシュ利用
- Web検索削減
- AI判定削減
- レスポンス高速化

## Phase 7｜公開運用高度化

- 店舗情報更新
- 利用履歴
- お気に入り
- 好み
- 検索履歴
- 店舗評価
- 将来的な予約連携等

---

# 26. 現在地

現在：

**Phase 1｜Ramen完成待ち**

Gourmetについて今やること：

- Overview / WorkLogを整備
- 既存コードを保持
- 大規模な機能追加はしない

Gourmetについて今やらないこと：

- 居酒屋版の本格DB構築
- 新しい検索エンジンへの大規模改修
- Ramenと並行した大量機能開発

これらはRamen完成後に実施する。

---

# 27. 次に再開するとき

Gourmet本格開発を再開するときは、最初にRamenの完成版設計を確認する。

特に確認するもの：

1. Ramenの最新Overview
2. Ramenの最新WorkLog
3. 店舗DB構造
4. 成長DB構造
5. Web新店舗発見
6. TTL
7. 候補数制御
8. AI判定キャッシュ
9. APIタイムアウト
10. 検索速度改善
11. LINE UI
12. 本番運用上の問題と解決策

その上で、

**Ramenで成功した設計だけをGourmetへ横展開する。**

---

# 28. 最重要条件

AIアヤフミ（グルメ）は、

**名前は「AIアヤフミ（グルメ）」のまま維持する。**

専門領域は、

**居酒屋・飲み屋検索**

とする。

完成イメージは、

**「AIアヤフミ（ラーメン）の居酒屋版」**

とする。

本格開発は、

**AIアヤフミ（ラーメン）の主要機能・検索基盤が完成してから開始する。**

既存Gourmetコードは捨てず、将来の再構築に利用する。

正常に動いている既存機能を壊さず、Ramenで実証済みの仕組みを安全に横展開する。

可能な限り無料・低コストで運用する。

---

# 29. AIアヤフミシリーズ共通・安全更新プロトコル

対象プロジェクト：`AIアヤフミ（グルメ）`

主なPython本体：`gourmet_bot.py`

AIアヤフミシリーズでは、ChatGPTとの開発時にファイルのダウンロード・手動上書きを毎回行う方式を原則としない。

基本思想：

**PowerShellへ貼るだけで更新できる状態を目指すが、利便性よりコード破損防止を優先する。**

## 1. Pythonコード更新

小規模・中規模のPython修正では、ChatGPTが現在の実コードを確認したうえでGit互換patchを生成する方式を基本とする。

基本フロー：

```text
現在のGit状態・実コード確認
↓
変更前 py_compile
↓
未commit差分確認
↓
必要に応じて一時バックアップ
↓
ChatGPTがpatch生成
↓
Base64等の文字化けしにくい形式でPowerShellへ渡す
↓
git apply --check
↓
問題がなければgit apply
↓
UTF-8読込確認
↓
変更後 py_compile
↓
git diff --check
↓
git diff
↓
単体テスト
↓
必要ならAPI / Render / LINE本番確認
↓
変更ファイルだけ明示的stage
↓
commit / push
```

ChatGPTは関数名・変数名・既存処理を推測してpatchを作らず、現在の実コードを確認してから修正する。

未commitの変更が存在する場合は、その差分を確認せずにpatchを重ねない。

## 2. 文字コード保護

過去にPowerShell経由の日本語文字列処理でPythonコードが文字化け・破損した事例があるため、文字コード保護を最重要ルールとする。

原則禁止：

- Pythonファイル全体を `Set-Content` で直接上書きする
- Pythonファイル全体を `Out-File` で直接上書きする
- 日本語を含むPythonコードをPowerShell文字列置換で直接編集する
- 日本語コードをhere-stringから直接Pythonへpipeして上書きする
- 変更前確認なしで一括置換する

基本：

- PythonソースはUTF-8を維持する
- patchの転送にはBase64等を利用し、PowerShellによる再エンコードを避ける
- `git apply --check` 成功前には元ファイルを変更しない
- 適用後は必ず `py_compile` を行う
- `git diff --check` と `git diff` で意図しない文字化け・大量変更・空白異常がないことを確認する
- PowerShell表示だけの文字化けと、実ファイルの破損を区別する

## 3. コード更新方式の優先順位

```text
1. 小規模修正
   → Git patch + PowerShell

2. 中規模修正
   → patchを複数に分割 + PowerShell

3. 大規模修正
   → 一時ファイル生成
   → py_compile
   → 差分確認
   → 検証後に安全に置換

4. 上記が不適切な場合のみ
   → 完全版ファイルのダウンロード
```

大規模なファイル全面書き換えにpatch方式を無理に使用しない。

完全版方式を使用する場合も、元ファイルへ直接上書きせず、別ファイルとして生成・検証してから置換する。

## 4. Overview更新

Overviewは、現在の正しい仕様・構成・設計・主要機能・ロードマップ・現在地をまとめる正本。

原則：

- 既存の該当セクションを更新する
- 「最新状態」セクションを末尾へ無限追加しない
- ChatGPTがPowerShell直貼り用更新コマンドを生成する
- 更新前に自動バックアップを作成する
- UTF-8を維持する
- 更新後に `git diff` で内容を確認する

## 5. WorkLog更新

WorkLogはチャット単位の開発作業履歴。

原則：

- 過去ログは原則書き換えない
- 上が古い / 下が新しい時系列を維持する
- 新しい作業記録は末尾へ追記する
- ChatGPTがPowerShell直貼り用追記コマンドを生成する
- 更新前に自動バックアップを作成する
- UTF-8を維持する

## 6. Git

`git add .` は原則使用しない。

変更したファイルだけを明示的にstageする。

commit前に原則として以下を確認する。

```powershell
git status --short
git diff --check
git diff
git diff --cached --name-status
```

秘密情報、APIキー、token、`.env`、個人データがstageされていないことを確認する。

## 7. チャット終了時の基本運用

ユーザーが「終了処理して」等と指示した場合、ChatGPTは原則として以下を行う。

1. そのチャットで行った変更を整理する
2. Overviewで更新が必要な既存箇所を特定する
3. Overview更新内容を作成する
4. WorkLogへ追記する作業記録を作成する
5. バックアップを含むPowerShell直貼り用更新コマンドを生成する
6. 更新後のGit差分確認コマンドを提示する
7. 問題がなければ変更ファイルだけをstageするコマンドを提示する
8. commit / pushを行う
9. 必要な場合はRender / LINE等の本番確認を行う

Python修正時も可能な限りファイルダウンロードではなく、安全なPowerShell更新方式を優先する。

---

最重要：

**手順を減らすために安全確認を省略しない。**

**正常に動いているコードを守り、文字化け・意図しない全体上書き・未確認patch適用を防止する。**
